import { PipeProductoListadoPipe } from './pipe-producto-listado.pipe';

describe('PipeProductoListadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProductoListadoPipe();
    expect(pipe).toBeTruthy();
  });
});
